name: 在 ubuntu-24.04 容器里编译 .AppImage（v3.8.3 分支专用）

on:
  workflow_dispatch:

jobs:
  build:
    # ubuntu-latest 默认装好 fuse 和 unsquashfs，能直接跑 AppImage
    runs-on: ubuntu-24.04

    steps:
      - name: 拉取代码（v3.8.3 分支）
        uses: actions/checkout@v4
        with:
          ref: v3.8.3

      - name: 安装编译依赖 + AppImage 工具
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake ninja-build g++ make \
            qtbase5-dev qttools5-dev-tools libqt5printsupport5-dev libqt5xmlpatterns5-dev \
            libfuse2 squashfs-tools \
            curl file zsync

      - name: 拉取 linuxdeploy 工具链
        run: |
          cd /tmp
          curl -L -o linuxdeploy-x86_64.AppImage \
            https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          curl -L -o linuxdeploy-plugin-qt-x86_64.AppImage \
            https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-x86_64.AppImage
          
          chmod +x linuxdeploy-x86_64.AppImage linuxdeploy-plugin-qt-x86_64.AppImage
          
          # 测试：ubuntu-24.04 的 fuse2 让 AppImage 直接可执行
          ./linuxdeploy-x86_64.AppImage --version
          ./linuxdeploy-plugin-qt-x86_64.AppImage --version

      - name: 配置 cmake
        run: |
          mkdir -p build && cd build
          cmake -S ../how_build -B . -G Ninja \
            -DCMAKE_BUILD_TYPE=Release

      - name: 编译源码
        run: |
          cd build
          cmake --build . --parallel

      - name: 准备 AppDir
        run: |
          mkdir -p AppDir/usr/bin
          mkdir -p AppDir/usr/share/applications
          mkdir -p AppDir/usr/share/icons/hicolor/128x128/apps
          mkdir -p AppDir/usr/share/metainfo
          
          cp build/NotePad-- AppDir/usr/bin/NotePad--
          chmod +x AppDir/usr/bin/NotePad--
          
          cp -r src/linux/usr/share/applications/* AppDir/usr/share/applications/
          cp -r src/linux/usr/share/icons/hicolor/128x128/apps/* AppDir/usr/share/icons/hicolor/128x128/apps/
          cp -r src/linux/usr/share/metainfo/* AppDir/usr/share/metainfo/ 2>/dev/null || true

      - name: 用 linuxdeploy 生成 AppImage
        env:
          QT_SELECT: qt5
          LINUXDEPLOY_PLUGIN_QT_VERSION: 5
        run: |
          cd AppDir
          export QML_SOURCES_PATHS=$(pwd)
          
          # 在 PATH 里能找到 linuxdeploy-plugin-qt
          export PATH="/tmp:$PATH"
          
          /tmp/linuxdeploy-x86_64.AppImage \
            --appdir . \
            --executable usr/bin/NotePad-- \
            --desktop-file usr/share/applications/NotePad--.desktop \
            --icon-file usr/share/icons/hicolor/128x128/apps/notepad--.png \
            --plugin qt \
            --output appimage
          
          echo "---生成的 AppImage---"
          ls -lh *.AppImage

      - name: 上传 AppImage 产物
        uses: actions/upload-artifact@v4
        with:
          name: notepad-appimage-v3.8.3
          path: AppDir/*.AppImage
          if-no-files-found: error